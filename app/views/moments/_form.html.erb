<%= form_with(model: moment, local: true) do |form| %>
  <% if moment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(moment.errors.count, "error") %> prohibited this moment from being saved:</h2>

      <ul>
      <% moment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :good %>
    <%= form.text_field :good, id: :moment_good %>
  </div>

  <div class="field">
    <%= form.label :bad %>
    <%= form.text_field :bad, id: :moment_bad %>
  </div>

  <div class="field">
    <%= form.label :okay %>
    <%= form.text_field :okay, id: :moment_okay %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
